version: "3.9"

services:
  rabbitmq-test:
    image: rabbitmq:4.2-management
    ports:
      - "5673:5672"
      - "15673:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
